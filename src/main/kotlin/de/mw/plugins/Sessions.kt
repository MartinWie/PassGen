package de.mw.plugins

import io.ktor.server.application.*
import io.ktor.server.sessions.*

/* Keep for later
fun Application.configureSessions() {
    install(Sessions) {
        cookie<UserSession>("user_session") {
            cookie.path = "/"
        }
        cookie<SaasSession>("saas_session") {
            cookie.path = "/"
        }
    }
}*/
